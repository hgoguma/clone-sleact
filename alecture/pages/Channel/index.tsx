import React from 'react';
import Workspace from '@layouts/Workspace'

const Channel = () => {
    return (
        <div>
            로그인 성공
            <Workspace />
        </div>
    )
}

export default Channel;